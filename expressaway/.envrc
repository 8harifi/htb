export NAME="expressaway"
export TARGET="10.129.2.26"
export LHOST="$(ip -4 route get 1.1.1.1 2>/dev/null | awk '{print $7; exit}')"
export NOTES="$PWD/notes.md"
export LOOT="$PWD/loot"
export SCANS="$PWD/scans"
mkdir -p "$LOOT" "$SCANS"
